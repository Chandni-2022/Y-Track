// const express = require('express');
// const router = express.Router();
// const { createProject, inviteMembers } = require('../controllers/projectController'); // Adjust the path if necessary

// // Route for creating a new project
// router.post('/', createProject);

// // Route for inviting members to a project (optional if you keep inviteMembers function)
// router.post('/:projectId/invite', inviteMembers);

// module.exports = router;
